# Base44 config — PUBLIC values only (baked into the app bundle at runtime).
# Secrets (ADMIN_TOKEN, RECAPTCHA_SECRET) are delivered via /run/base44/app.env.

# Google reCAPTCHA v3 site key (public, client-side).
VITE_RECAPTCHA_SITE_KEY=6Lc_s6ctAAAAAAP7In69-LKpeGGUGFQ8UCyfW0kd

# Cloudflare Turnstile site key (public, client-side).
VITE_TURNSTILE_SITE_KEY=0x4AAAAAADBVJXDKno5ekmDP

# Backend URL (overridden in compose to the local backend for the preview;
# production points to the Cloudflare Worker: https://gosuksa-edge.bcare.workers.dev)
VITE_BACKEND_WS_URL=https://gosuksa-edge.bcare.workers.dev
