# Base44 frontend build config — PUBLIC values only, baked in at build time.
# (The Cloudflare Worker secrets ADMIN_API_TOKEN / ENCRYPTION_KEY_B64 /
#  TURNSTILE_SECRET_KEY must never appear in frontend code or this file.)

# Public Cloudflare Worker API — the system of record.
VITE_API_BASE_URL=https://becaree-api.jacgosu.workers.dev

# Public Cloudflare Turnstile site key (client-side widget only).
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAElb6QB9gr3LB6oB
